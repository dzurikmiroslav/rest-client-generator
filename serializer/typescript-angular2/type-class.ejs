export interface <%- capitalize(type.name) %> {<% type.fields.forEach(function(field) { %>
    <%- field.name %>: <%- field.basic ? field.type : capitalize(field.type) %><%- field.array ? '[]' : '' %>;<% }); 
%>
}